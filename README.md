## LingoVibe

Next.js + Tailwind starter for a language learning app.

### What’s included

- **Beautiful landing page** with 3 core features:
  - **Custom text**
  - **Daily news**
  - **Grammar**
- **App Router** (`src/app`)
- **Reusable UI primitives** (`src/components/ui`)
- **Landing sections** (`src/components/landing`)
- Simple utility helpers (`src/lib`)

### Run locally

1. Install Node.js (18+ recommended).
2. Install deps and start:

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

